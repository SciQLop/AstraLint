name: MandatoryVariableAttributes
description: "All variables must have required ISTP variable attributes"
url: "https://spdf.gsfc.nasa.gov/istp_guide/vattributes.html"
reference: "ISTP-VA-001"
severity: ERROR
suite: ISTP

assertions:
  - path: "variables/.*/attributes"
    check: contains_keys
    keys:
      - CATDESC
      - FIELDNAM
      - VAR_TYPE
    message: "{% if valid %}{{ variable }} has all mandatory attributes{% else %}{{ variable }} is missing mandatory attribute(s): {{ missing_keys | join(', ') }}{% endif %}"

  - check: one_of
    assertions:
      - path: "variables/.*/attributes"
        check: contains_keys
        keys:
          - FORMAT
        message: "Variable must have either FORMAT or FORM_PTR attribute"
      - path: "variables/.*/attributes"
        check: contains_keys
        keys:
          - FORM_PTR
        message: "Variable must have either FORMAT or FORM_PTR attribute"

  - check: if_then
    if:
      path: "variables/.*/record_variance"
      check: comparison
      operator: "="
      value: true
    then:
      path: "variables/.*/attributes"
      check: contains_keys
      keys:
        - FILLVAL
      message: "Record-varying variable must have FILLVAL attribute"
