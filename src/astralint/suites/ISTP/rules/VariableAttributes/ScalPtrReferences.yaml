name: ScalPtrReferences
description: "SCAL_PTR must reference an existing variable"
url: "https://spdf.gsfc.nasa.gov/istp_guide/vattributes.html#SCAL_PTR"
reference: "ISTP-VA-018"
severity: ERROR
suite: ISTP

assertions:
  - path: "variables/.*/attributes/SCAL_PTR/values/[0-9]*"
    check: reference_variable
    error_if_no_match: false
    message: "{% if valid %}Attribute SCAL_PTR of {{ variable }} references existing variable '{{ value }}'{% else %}Attribute SCAL_PTR of {{ variable }} references non-existent variable '{{ value }}'{% endif %}"
